{
  manifest_version: "0.1.0",

  config_schema: {
    type: "object",
    properties: {
      judge_llm: { type: "string", default: "openai/gpt-4o-mini" },
      openai_api_key: { type: "string", secret: true, default: "" },
      gemini_api_key: { type: "string", secret: true, default: "" },
      deepseek_api_key: { type: "string", secret: true, default: "" },
    },
    required: [],
    additionalProperties: false,
  },

  slots: {
    proxy: { kind: "a2a" },
  },

  program: {
    image: "ghcr.io/andrew7234/mind2web2-agentbeats:latest",
    entrypoint: "xvfb-run uv run src/server.py --host 0.0.0.0 --port 9009",

    env: {
      PYTHONUNBUFFERED: "1",
      AMBER_HINT_PROXY: "${slots.proxy}",
      OPENAI_API_KEY: "${config.openai_api_key}",
      GEMINI_API_KEY: "${config.gemini_api_key}",
      DEEPSEEK_API_KEY: "${config.deepseek_api_key}",
      AGENT_LLM: "${config.judge_llm}",
      LOG_LEVEL: "INFO",
    },

    network: {
      endpoints: [
        { name: "a2a_endpoint", port: 9009 },
      ],
    },
  },

  provides: {
    a2a: { kind: "a2a", endpoint: "a2a_endpoint" },
  },

  exports: { a2a: "a2a" },

  metadata: {
    assessment_config: {
      domain: "dev_set",
    },
    participant_roles: ["agent"],
  },
}
